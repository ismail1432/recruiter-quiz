{% extends 'base.html.twig' %}

{% block body %}

    <div class="example-wrapper">
        <h1>Check your result and the answers!</h1>
        <h3>You have {{ score }}% of good answers</h3>

        <ul>
            {% for correction in result.corrections %}
                <li>{{ correction.isCorrect ? '✅' : '❌' }} Question: {{ correction.question }}</li>
                <li>Your answer: {{ correction.submittedAnswer ?? '🕳️' }}</li>
                <li>The response: {{ correction.response }}</li>
            {% endfor %}
        </ul>

    </div>
{% endblock %}
